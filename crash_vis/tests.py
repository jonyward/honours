from django.test import TestCase

#This really needs done, really only needs to test whether or not the model migrations have worked, and that the table in the database contains the data, as well as checking
#if pages are properly loading/maps being created in the view
